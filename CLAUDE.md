# Claude Code instructions

- Always apply the `ponytail` skill by default for any coding task (writing, adding, refactoring, fixing, reviewing, or designing code) — invoke it before starting the task, not just when the user says "ponytail".
- Always apply the `caveman` skill by default for all replies — invoke it at the start of the session/turn, not just when the user says "caveman mode".
